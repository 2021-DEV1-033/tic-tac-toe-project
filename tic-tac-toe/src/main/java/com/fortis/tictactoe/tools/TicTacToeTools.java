package com.fortis.tictactoe.tools;


/**
 * The globally used methods shared with all the project to be used when needed
 * 
 * @author the developer
 *
 */
public abstract class TicTacToeTools {
	public static boolean isNullOrEmpty(String string) {
		return string == null || string.isEmpty();
	}
}
